# Bot token จาก Discord Developer Portal → Application → Bot
DISCORD_TOKEN=your_bot_token_here

# ใช้เฉพาะตอนรัน npm run deploy เพื่อลงทะเบียน slash commands (ถ้าไม่ใส่ core จะใช้ client.user.id ตอน ready)
CLIENT_ID=your_application_id_here
